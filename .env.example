DATABASE_URL=postgresql://localhost:5432/api_logs
PORT=8080
HOST=0.0.0.0

# Retention / analytics job (started with the API server)
LOG_RETENTION_DAYS=14
# Calendar used for daily rollups — set to your primary team timezone
ANALYTICS_TIMEZONE=Asia/Kolkata
PRUNE_JOB_INTERVAL_MS=3600000
# PRUNE_JOB_ENABLED=0
